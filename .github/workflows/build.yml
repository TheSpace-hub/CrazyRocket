name: Build game
on: push
jobs:
  create_executable_file:
    name: Create executable file.
    runs-on: ubuntu-latest
    steps:
      - name: Write ENV value
        run: echo "HELLO_WORLD_V=hiPlanet" >> $GITHUB_ENV

      - name: Read ENV value
        run: echo "::notice::This word is $HELLO_WORLD_V"
