name: Build game
on: push
jobs:
  create_executable_file:
    name: Create executable file.
    runs-on: ubuntu-latest
    steps:
      - name: Write ENV value
        run: echo "### Well done! That's works!" >> $GITHUB_STEP_SUMMARY
